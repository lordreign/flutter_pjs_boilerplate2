import 'package:flutter/material.dart';

// 주색상
const PRIMARY_COLOR = Color(0xFF22A45D);

// 배경 색상
const BACKGROUND_COLOR = Color(0xFF414141);

// 텍스트 (기본)
const TEXT_COLOR = Color(0xFFFFFFFF);

// 텍스트 (보조) 중간톤, 설명문 등
const TEXT_SUB_COLOR = Color(0xFFB0B0B0);

// 테두리
const BORDER_COLOR = Color(0xFF5A5A5A);

// 입력필드 색상
const INPUT_FIELD_COLOR = Color(0xFF2C2C2C);

// 포커스 색상
const FOCUS_COLOR = Color(0xFF00D8FF);
